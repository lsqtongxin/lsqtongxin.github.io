<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>myFirstArticle</title>
    <url>/2021/04/28/myFirstArticle/</url>
    <content><![CDATA[<h2 id="这是第一篇文章"><a href="#这是第一篇文章" class="headerlink" title="这是第一篇文章"></a>这是第一篇文章</h2><p>很多年前就开始写blog了，从csdn到Jekyll，再到自己使用PHP/YAF搭建网站，使用的平台也很多了，包括之前的新浪云SAE、百度云、腾讯云、阿里云，最后选择了Hexo</p>
<p><img src="/2021/04/28/myFirstArticle/tangying.jpg" alt="躺赢"></p>
<p><img src="/2021/04/28/myFirstArticle/gugong.jpg" alt="故宫"></p>
]]></content>
  </entry>
  <entry>
    <title>myTestArticle</title>
    <url>/2021/04/21/myTestArticle/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
  </entry>
  <entry>
    <title>工作中Git的那些操作</title>
    <url>/2021/06/19/%E5%B7%A5%E4%BD%9C%E4%B8%ADGit%E7%9A%84%E9%82%A3%E4%BA%9B%E6%93%8D%E4%BD%9C/</url>
    <content><![CDATA[<p>其实接触Git也很长时间了，自打上学那会就用Git作为版本控制工具，感触颇深。写这篇文章也是记录一下自己的理解留作日后词典进行查询，另外也是想把这些内容分享给大家。本篇将以问题为导向来阐述相关的知识，，面对的对象为具有一定经验的开发者。</p>
<p>假设当前项目有两个分支，为master和dev，所有开发工作都在dev上进行，而master分支为用于发布生产代码的分支，当前系统为windows7或windows10</p>
<p><strong>我整理了我在日常工作中的十个问题，重点是第4个问题和第8个问题，如下所示：</strong></p>
<h3 id="1-撤销本地工作目录中的更改"><a href="#1-撤销本地工作目录中的更改" class="headerlink" title="1.撤销本地工作目录中的更改"></a>1.撤销本地工作目录中的更改</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git checkout -- &lt;file&gt;</span><br></pre></td></tr></table></figure>

<p>这样将撤销对本地工作目录的更改。</p>
<h3 id="2-撤销本地缓存的更改"><a href="#2-撤销本地缓存的更改" class="headerlink" title="2.撤销本地缓存的更改"></a>2.撤销本地缓存的更改</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git reset &lt;file&gt;`								//从缓存区移除特定文件，但不改变工作目录，与`git add &lt;file&gt;`是反义词</span><br><span class="line">git reset											  //它会取消 *所有* 文件的缓存，而不会覆盖任何修改，与`git add .`是反义词</span><br><span class="line">git reset --hard						    //它会取消 *所有* 文件的缓存，并且恢复工作目录</span><br></pre></td></tr></table></figure>

<h3 id="3-当在本地提交commit后如何修改这个commit-message？"><a href="#3-当在本地提交commit后如何修改这个commit-message？" class="headerlink" title="3.当在本地提交commit后如何修改这个commit message？"></a>3.当在本地提交commit后如何修改这个commit message？</h3><p><strong>git commit命令有一个amend参数，用于修改之前的提交信息</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git commit --amend</span><br></pre></td></tr></table></figure>

<p>当点击回车后，会出现一个编辑框，重新编辑内容并保存退出即可。</p>
<p>其实还有一个场景是：你写的commit message是对的，但是你忘记了修改了某个内容，那怎么办？</p>
<p>这时候你有两个方法，第一就是重新修改然后add和commit，即生成一个新的节点。</p>
<p>第二种就是在当前这个节点上修改然后add，最后amend，即在原来的节点进行修改。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">vim  test.txt</span><br><span class="line"></span><br><span class="line">git add . </span><br><span class="line"></span><br><span class="line">git commit --amend</span><br></pre></td></tr></table></figure>

<p>这是一种将忘记的修改添加到当前这个节点的方法</p>
<p><strong>总结：amend这个参数不仅仅能修改commit message，还能对修改进行更正</strong></p>
<h3 id="4-当周三升级要对某个版本进行打包"><a href="#4-当周三升级要对某个版本进行打包" class="headerlink" title="4.当周三升级要对某个版本进行打包"></a>4.当周三升级要对某个版本进行打包</h3><p>首先通过<code>git log</code>命令查询当前的历史，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">commit ae4fa4a6d8f9b4f6095c822935e3e0f7ab20f7cd (HEAD -&gt; master)</span><br><span class="line">Author: lsqtongxin &lt;lsqtongxin@qq.com&gt;</span><br><span class="line">Date:   Sat Jun 19 11:09:06 2021 +0800</span><br><span class="line"></span><br><span class="line">    add third.java</span><br><span class="line"></span><br><span class="line">commit 1cf533f4bca614a163810da46657efc9343075d5</span><br><span class="line">Author: lsqtongxin &lt;lsqtongxin@qq.com&gt;</span><br><span class="line">Date:   Sat Jun 19 11:08:18 2021 +0800</span><br><span class="line"></span><br><span class="line">    add second.java</span><br><span class="line"></span><br><span class="line">commit 9d69109d65ffd13291c3f72f304e36a20b99b32b</span><br><span class="line">Author: lsqtongxin &lt;lsqtongxin@qq.com&gt;</span><br><span class="line">Date:   Sat Jun 19 11:06:48 2021 +0800</span><br><span class="line"></span><br><span class="line">    add main.java</span><br></pre></td></tr></table></figure>

<p>当你想使用second.java(1cf533f)来进行周三生产升级，但是当前的最新的节点为third.java(ae4fa4a)它是你最近完成的代码内容，即你电脑硬盘里显示为third.java版本。那你如何操作呢？</p>
<p>使用git checkout <commitID>命令来针对某个版本进行检出代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git checkout 1cf533f                  //这样你的硬盘将会恢复为second.java文件等</span><br></pre></td></tr></table></figure>

<p>然后进行代码的打包、编译、测试即可，待工作完成即可。</p>
<p><strong>又有人问，我第二天上班时候，如何恢复到最新的状态呢？</strong></p>
<p>通过git log是没法找到third.java的commitID的，那么如何找到third.java的commitID呢？</p>
<p>假设我们刚刚是在master上进行操作，所以最新的third.java节点位于master分支上。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git checkout master                    //这样将会使恢复如初。</span><br></pre></td></tr></table></figure>

<p><strong>总结： <code>git checkout &lt;commitID&gt;</code> 本质上是通过改变HEAD的指向来进行移动节点，它的移动的颗粒度为某个commit版本 ，在多分支操作中，这个命令还用于切换分支</strong></p>
<h3 id="5-开发某个代码时想看一下之前的版本中某个文件"><a href="#5-开发某个代码时想看一下之前的版本中某个文件" class="headerlink" title="5. 开发某个代码时想看一下之前的版本中某个文件"></a>5. 开发某个代码时想看一下之前的版本中某个文件</h3><p>接上面的第4步骤中的<code>git log</code>,例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git checkout &lt;commit&gt; &lt;file&gt;</span><br></pre></td></tr></table></figure>

<p>查看文件之前的版本。它将工作目录中的 <code>&lt;file&gt;</code> 文件变成 <code>&lt;commit&gt;</code> 中那个文件的拷贝，并将它加入缓存区。</p>
<p>那么如何返回最新的版本的file呢？</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git chekcout HEAD &lt;file&gt;</span><br></pre></td></tr></table></figure>

<p><strong>总结： 相比较于第4步骤，它的移动的颗粒度为某个commit版本的某个文件</strong></p>
<h3 id="6-合并你的多个提交-压缩提交"><a href="#6-合并你的多个提交-压缩提交" class="headerlink" title="6.合并你的多个提交(压缩提交)"></a>6.合并你的多个提交(压缩提交)</h3><p>假设你在dev分支上，新建一个feature分支开发一个功能点，计划5个工作日完成，你为了不丢失每天的工作内容，每天在本地commit一个提交来记录你当天的工作，那么你就有了5个commitID，导致你后续会因为开发很多功能点会有很多个commitID，有的功能点需要10个工作日即10个commitID，你会发现这些commit非常繁杂。</p>
<p>那么我要是按照功能点的颗粒度，是否可以将这5个commitID或者10个commitID合并为一个commitID来呢？</p>
<p>这样会将你的历史很简洁、干净和清爽，翻阅过往的历史也会很快速。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git rebase -i &lt;base&gt;								//在feature分支上</span><br></pre></td></tr></table></figure>

<p>其中<code>&lt;base&gt;</code>为一个基准点，为某一个commitID，都是以这个基准点进行合并，但是合并的内容不包括这个基准点。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git rebase -i cf375ac							  //在feature分支上</span><br></pre></td></tr></table></figure>

<p>假设我们的历史从古至今为：25ccabd-&gt;0134238-&gt;d46d81e，也就是合并这三个节点为一个节点。</p>
<p>25ccabd为feature的第一天工作</p>
<p>0134238为feature的第二天工作</p>
<p>d46d81e为feature的第三天工作</p>
<p>而这个基准点为cf375ac，这个commitID cf375ac内容等等均不会变化。这里一定注意参数这不是25ccabd,然后将0134238和d46d81e前面文字由pick改为squash或者s，保存退出，然后再重新写提交信息，最后保存退出即可。</p>
<h3 id="7-拉取更新"><a href="#7-拉取更新" class="headerlink" title="7.拉取更新"></a>7.拉取更新</h3><p>先 fetch，然后 merge。<code>git pull</code> 命令是整合了这两个过程的方式。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git fetch &lt;remote&gt; &lt;branch&gt;          //从远程拉取某个分支的更新，`git fetch &lt;remote&gt;`会拉取所有分支</span><br></pre></td></tr></table></figure>

<p>例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git fetch origin dev						     //拉取远程origin/dev分支的更新</span><br><span class="line"></span><br><span class="line">git branch -r											   //查看远程分支</span><br><span class="line"></span><br><span class="line">git checkout dev								 	   //切换到dev分支</span><br><span class="line"></span><br><span class="line">git merge origin/dev						     //将远程origin/dev合并到本地dev分支上</span><br></pre></td></tr></table></figure>

<p>于是我们得到了远程origin/dev的更新，将其他同事的commit更新到了我的本地。</p>
<p><code>git pull</code>是上面两个过程的整合，可以参考第8步。</p>
<h3 id="8-分支合并"><a href="#8-分支合并" class="headerlink" title="8.分支合并"></a>8.分支合并</h3><p>假设我们基于dev分支开发某一个feature功能点：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git branch feature dev							//基于dev新建一个feature分支</span><br><span class="line">git checkout feature								//切换到feature分支进行开发工作</span><br></pre></td></tr></table></figure>

<p>当多人协作时候，有许多人都合并了某个feature分支到dev分支上，导致dev分支有很多更新。</p>
<p>那么当我们完成了我们的feature功能后，我们如何操作呢？</p>
<p>首先我们应该远程拉取dev分支的更新:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git checkout dev										 //切换到dev分支</span><br><span class="line">git pull origin 										 //拉取远程origin的dev分支的更新</span><br></pre></td></tr></table></figure>

<blockquote>
<p>如果本地dev已经提交了更新，那么需要合并本地dev和远程dev：<code>git pull --rebase origin</code></p>
</blockquote>
<p>然后在feature分支上进行rebase操作：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git rebase -i dev										//这一步类似第6步骤</span><br></pre></td></tr></table></figure>

<p>最后切换到dev分支进行合并：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git checkout dev</span><br><span class="line">git merge feature</span><br></pre></td></tr></table></figure>

<p>再进行将dev分支推送到远程主机</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git push origin dev</span><br></pre></td></tr></table></figure>

<p><strong>总结：</strong></p>
<p><strong>整体步骤分为第一pull：先拉取远程dev的更新，</strong></p>
<p><strong>第二rebase：再基于dev进行 rebase，</strong></p>
<p><strong>第三merge，最后将feature合并到dev分支。</strong></p>
<p><strong>第四是push</strong></p>
<h3 id="9-撤销某次commit的更改"><a href="#9-撤销某次commit的更改" class="headerlink" title="9.撤销某次commit的更改"></a>9.撤销某次commit的更改</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git revert &lt;commitID&gt;</span><br></pre></td></tr></table></figure>

<p>这个命令的重点会再生成一个新的节点，并且不会删除这个旧的节点，这样避免了丢失项目历史。</p>
<p>一般用于当某个历史提交引入了bug，一般是解决这个bug并且再生成一个新的节点。或者是将那个节点revert退回。</p>
<h3 id="10-移除某次commit到目前的更改"><a href="#10-移除某次commit到目前的更改" class="headerlink" title="10.移除某次commit到目前的更改"></a>10.移除某次commit到目前的更改</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git reset &lt;commit&gt;</span><br></pre></td></tr></table></figure>

<p>将当前分支的末端移到 <code>&lt;commit&gt;</code>，将缓存区重设到这个提交，但不改变工作目录。所有 <code>&lt;commit&gt;</code> 之后的更改会保留在工作目录中，这允许你用更干净、原子性的快照重新提交项目历史。可以和rebase操作类似。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git reset --hard &lt;commit&gt;</span><br></pre></td></tr></table></figure>

<p>将当前分支的末端移到 <code>&lt;commit&gt;</code>，将缓存区和工作目录都重设到这个提交。它不仅清除了未提交的更改，同时还清除了 <code>&lt;commit&gt;</code> 之后的所有提交</p>
]]></content>
  </entry>
</search>
